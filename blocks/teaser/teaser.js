/**
 * Teaser — promo/media+copy unit (scdemos spectrum).
 * Authored DOM is the structure; decoration is intentionally minimal.
 */
export default function decorate(block) {
  block.classList.add('teaser-ready');
}
