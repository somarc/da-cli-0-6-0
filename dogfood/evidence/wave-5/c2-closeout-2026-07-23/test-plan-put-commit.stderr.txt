Diff for /test-plan.html:
-             <p><strong>Last updated:</strong> 2026-07-23 13:42 UTC · keep this stamp current whenever the plan or progress log changes.</p>
+             <p><strong>Last updated:</strong> 2026-07-23 16:41 UTC · keep this stamp current whenever the plan or progress log changes.</p>
-       <p>As of <strong>2026-07-23 13:42 UTC</strong>:</p>
+       <p>As of <strong>2026-07-23 16:41 UTC</strong>:</p>
-           <div><p><strong>Cut 2026-07-17.</strong> Full drill matrix: A1–A3, B1–B3, C1, C3, D3, D4 closed; D1/D2 banked from Wave 4; C2 recognized+contained+recovered with the final orphan-removal rep alone deferred on the Helix-admin delete permission (operator action, tracked). Ten findings — <a href="/learnings/f027-list-success-bare-array">f027</a>–<a href="/learnings/f036-approval-gate-zombie">f036</a> — every drill surfaced something real, all fixed/recovered same-session. Drill matrix: <code>dogfood/WAVES.md</code>; evidence: <code>dogfood/evidence/wave-5/</code>.</p></div>
+           <div><p><strong>Cut 2026-07-17; C2 closed 2026-07-23.</strong> Full drill matrix: A1–A3, B1–B3, C1–C3, D3, D4 closed; D1/D2 banked from Wave 4. The final isolated C2 rep proved a contentbus route becoming a real orphan, guarded cleanup, successful Helix unpreview/unpublish, and DA/preview/live absence. Ten findings — <a href="/learnings/f027-list-success-bare-array">f027</a>–<a href="/learnings/f036-approval-gate-zombie">f036</a> — every drill surfaced something real, all fixed/recovered same-session. Drill matrix: <code>dogfood/WAVES.md</code>; evidence: <code>dogfood/evidence/wave-5/</code>.</p></div>
-           <div><p>2026-07-23 13:42</p></div>
+           <div><p>2026-07-23 16:41</p></div>
-           <div><p><strong>Wave 6 cut after the Configuration Service grant.</strong> The retained site-registration recovery returned <code>adopted-existing</code>; explicit target diagnostics confirmed a matching DA source; da-cli committed and previewed <code>/wave-6-proof.html</code>; rendered verification was non-empty, route ownership was contentbus, and audit full returned 0 errors/0 warnings. No live publication of the proof route occurred. The evidence pack now closes auth, skills, site lifecycle, and Sidekick read while preserving commerce and Stardust as honest external-conditionals.</p></div>
+           <div><p><strong>Wave 5 C2 final orphan-removal rep closed.</strong> The maintainer selected the executed path on the registered disposable site and isolated <code>proof-v0-6-0-replay</code> branch. A noncanonical draft route was established with DA source plus preview/live 200, source deletion produced a real orphan with delivery still present, dry-run planned exactly unpreview + unpublish, and committed <code>route clean</code> completed both Helix deletes with exit 0. Final probes proved DA source absent and preview/live/code all 404. No production or canonical route was used; redacted evidence is retained under <code>dogfood/evidence/wave-5/c2-closeout-2026-07-23/</code>.</p></div>
-           <div><p>2026-07-22 17:12</p></div>
+           <div><p>2026-07-23 13:42</p></div>
-           <div><p><strong>MEASURE · MATCH · QUALIFY Round 2 — eight blocks.</strong> A fixed 4.5 KB page exercised hero, cards, columns, quote, accordion, tabs, table, and teaser. Both GPT-5.6 Terra arms proved exact DA source, preview/plain HTTP 200, one H1, all eight authored and loaded blocks, twelve exact DOM transformations, zero block/console errors, and 16/16 JS/CSS assets; no live action. To remove Round 1's shared-browser confound, arms ran sequentially (Adobe first, da-cli second). da-cli used 407,942 versus 609,193 logical tokens (<strong>33.0% lower</strong>), 26,360 versus 60,029 uncached input (<strong>56.1% lower</strong>), 34.6% lower estimated model cost, 23 versus 26 rounds (<strong>11.5% lower</strong>), and 36.3% less wall time. Evidence: <a href="/benchmarks/token-efficiency/round-2">Round 2 result</a>, both specimens, the updated <a href="/data/token-efficiency.json">sheet</a>, and git <code>dogfood/evidence/token-efficiency/2026-07-22/round-2-multi-block/</code>. Two matched pairs remain descriptive, not inferential.</p></div>
+           <div><p><strong>Wave 6 cut after the Configuration Service grant.</strong> The retained site-registration recovery returned <code>adopted-existing</code>; explicit target diagnostics confirmed a matching DA source; da-cli committed and previewed <code>/wave-6-proof.html</code>; rendered verification was non-empty, route ownership was contentbus, and audit full returned 0 errors/0 warnings. No live publication of the proof route occurred. The evidence pack now closes auth, skills, site lifecycle, and Sidekick read while preserving commerce and Stardust as honest external-conditionals.</p></div>
-           <div><p>2026-07-22 16:13</p></div>
+           <div><p>2026-07-22 17:12</p></div>
-           <div><p><strong>Token-efficiency estimate became a measured proof.</strong> Two clean GPT-5.6 Terra agents executed the same fixed DA page-create-and-preview task concurrently: one through <code>da-cli</code>, one through Adobe public <code>da-auth</code>/<code>da-content</code> skills and documented APIs under a path-scoped preview-only exception. Both passed 5/5. In the first pair, da-cli used 401,283 versus 544,564 logical tokens (<strong>26.3% lower</strong>), 38,491 versus 73,895 uncached input (<strong>47.9% lower</strong>), and 28.9% lower estimated provider cost; wall time tied, while the CLI arm used more rounds because three browser checks failed before direct navigation. Public proof: <a href="/benchmarks/token-efficiency">benchmark</a>, both specimen pages, <a href="/data/token-efficiency.json">result sheet</a>, and git <code>dogfood/evidence/token-efficiency/2026-07-22/</code>. N=1 remains preliminary; no live endpoint was called.</p></div>
+           <div><p><strong>MEASURE · MATCH · QUALIFY Round 2 — eight blocks.</strong> A fixed 4.5 KB page exercised hero, cards, columns, quote, accordion, tabs, table, and teaser. Both GPT-5.6 Terra arms proved exact DA source, preview/plain HTTP 200, one H1, all eight authored and loaded blocks, twelve exact DOM transformations, zero block/console errors, and 16/16 JS/CSS assets; no live action. To remove Round 1's shared-browser confound, arms ran sequentially (Adobe first, da-cli second). da-cli used 407,942 versus 609,193 logical tokens (<strong>33.0% lower</strong>), 26,360 versus 60,029 uncached input (<strong>56.1% lower</strong>), 34.6% lower estimated model cost, 23 versus 26 rounds (<strong>11.5% lower</strong>), and 36.3% less wall time. Evidence: <a href="/benchmarks/token-efficiency/round-2">Round 2 result</a>, both specimens, the updated <a href="/data/token-efficiency.json">sheet</a>, and git <code>dogfood/evidence/token-efficiency/2026-07-22/round-2-multi-block/</code>. Two matched pairs remain descriptive, not inferential.</p></div>
-           <div><p>2026-07-22 01:32</p></div>
+           <div><p>2026-07-22 16:13</p></div>
-           <div><p><strong>DA source truth reconciled.</strong> Retired Git-authored HTML/JSON mirrors and the fixture-fed construct; checked-in <code>certify.yaml</code> now activates and validates authoritative DA source, while <code>promote.yaml</code> owns the separate live approval boundary. Historical evidence remains immutable.</p></div>
+           <div><p><strong>Token-efficiency estimate became a measured proof.</strong> Two clean GPT-5.6 Terra agents executed the same fixed DA page-create-and-preview task concurrently: one through <code>da-cli</code>, one through Adobe public <code>da-auth</code>/<code>da-content</code> skills and documented APIs under a path-scoped preview-only exception. Both passed 5/5. In the first pair, da-cli used 401,283 versus 544,564 logical tokens (<strong>26.3% lower</strong>), 38,491 versus 73,895 uncached input (<strong>47.9% lower</strong>), and 28.9% lower estimated provider cost; wall time tied, while the CLI arm used more rounds because three browser checks failed before direct navigation. Public proof: <a href="/benchmarks/token-efficiency">benchmark</a>, both specimen pages, <a href="/data/token-efficiency.json">result sheet</a>, and git <code>dogfood/evidence/token-efficiency/2026-07-22/</code>. N=1 remains preliminary; no live endpoint was called.</p></div>
-           <div><p>2026-07-21 17:24</p></div>
+           <div><p>2026-07-22 01:32</p></div>
-           <div><p><strong>f039 fixed — the registry caught a second metadata channel hiding in plain sight.</strong> The agent supplied complete-document head metadata plus the canonical authored Metadata block; pristine <code>9497674</code> scored it 100, committed it, previewed it, and exposed projected rows visibly. Platform research ruled out the arbitrary “ban head” fix: html2md intentionally supports head-only imports. <a href="/learnings/f039-duplicate-metadata-channels">f039</a> establishes one effective channel and one main as the source contract. PR #51/ADR 0004 enforce it across put/put-tree/workspace/Stardust/sourcePut, quality gate, and preview explain. Same draft: old gate 100/pass → fixed gate 80/fail; fixed put refuses before write; recognizer names all four visible rows; one-channel recovery is clean. 683/683, Node 22/24 and repo health green, release smoke green; merged <code>916484d</code>.</p></div>
+           <div><p><strong>DA source truth reconciled.</strong> Retired Git-authored HTML/JSON mirrors and the fixture-fed construct; checked-in <code>certify.yaml</code> now activates and validates authoritative DA source, while <code>promote.yaml</code> owns the separate live approval boundary. Historical evidence remains immutable.</p></div>
-           <div><p>2026-07-17 11:53</p></div>
+           <div><p>2026-07-21 17:24</p></div>
-           <div><p><strong>Wave 6 opened with the second-site proof.</strong> A clean boilerplate became <a href="https://main--da-lifecycle-atlas--somarc.aem.live/">DA Lifecycle Atlas</a> through the current source-tree CLI, not hand authoring: expired auth refused before the first write and recovered explicitly; an invalid slashy branch name was caught and renamed before construction; seven DA documents, six authored block families, nav/footer, plans, learnings, and provenance were installed; the constrained construct completed 18/18; block contracts reported <code>missing=[]</code>; audit full returned 0 errors/0 warnings; feature live reached 7/7 fresh. A reviewed Riverboat plan carried one explicitly approved Grok Imagine step with unsafe provenance retained; Grok Build 0.2.102 produced one 1280×720 plate; prompt/YAML/wrapper/binary/output/image hashes were captured; human review promoted the exact file; normal da-cli binary upload installed it in DA; the rendered optimized derivative is public with no <code>about:error</code>. This closes the second-site and credentialed-integration slices, not the whole wave. Evidence pointer: <code>dogfood/evidence/wave-6/</code>.</p></div>
+           <div><p><strong>f039 fixed — the registry caught a second metadata channel hiding in plain sight.</strong> The agent supplied complete-document head metadata plus the canonical authored Metadata block; pristine <code>9497674</code> scored it 100, committed it, previewed it, and exposed projected rows visibly. Platform research ruled out the arbitrary “ban head” fix: html2md intentionally supports head-only imports. <a href="/learnings/f039-duplicate-metadata-channels">f039</a> establishes one effective channel and one main as the source contract. PR #51/ADR 0004 enforce it across put/put-tree/workspace/Stardust/sourcePut, quality gate, and preview explain. Same draft: old gate 100/pass → fixed gate 80/fail; fixed put refuses before write; recognizer names all four visible rows; one-channel recovery is clean. 683/683, Node 22/24 and repo health green, release smoke green; merged <code>916484d</code>.</p></div>
-           <div><p>2026-07-17 02:15</p></div>
+           <div><p>2026-07-17 11:53</p></div>
-           <div><p><strong>Wave 5 cut — the completion drills ate three more primitives.</strong> B1 (source drift): the conflict guard was perfect and its recovery was a trap — <code>content merge</code> destroyed the modified local copy it told you to re-apply (<a href="/learnings/f034-merge-destroys-local-side">f034</a>; merge now preserves the local side under <code>.da/merge-backups/</code>, drift refusals are envelopes). B2 (invalid section shape): the strict door held but <code>--no-strict-sections</code> was a dead switch blaming an absent flag with empty json stdout (<a href="/learnings/f035-broken-off-switch">f035</a>); the flatten it guards against is <em>invisible</em> to rendered-side audits — <code>audit full</code> passed 0-errors over a destroyed block — and <code>preview explain</code> is the recognizer that names it. B3: <code>audit contracts --verify-code</code> named the missing asset exactly. D3 (approval): a closed-stdin gate exited <strong>0</strong> mid-run and stranded a zombie <code>running</code> state (<a href="/learnings/f036-approval-gate-zombie">f036</a>; approvals now explicit via <code>--approve/--approve-all</code>, non-TTY refusals fail fast, prompt on stderr). D4: partial batch failure was honest per-row but exited 0 as a bare array — now one envelope, exit 1. C2's unpublish leg re-probed: still 403, deferred on the operator grant, refusal envelope clean. da-cli 618/618, 7 new locks all verified failing on old code; every fix field-re-proven on this site. Wave 5 <strong>cut</strong> with the C2 leg documented; Wave 6 is the last wave before the gate.</p></div>
+           <div><p><strong>Wave 6 opened with the second-site proof.</strong> A clean boilerplate became <a href="https://main--da-lifecycle-atlas--somarc.aem.live/">DA Lifecycle Atlas</a> through the current source-tree CLI, not hand authoring: expired auth refused before the first write and recovered explicitly; an invalid slashy branch name was caught and renamed before construction; seven DA documents, six authored block families, nav/footer, plans, learnings, and provenance were installed; the constrained construct completed 18/18; block contracts reported <code>missing=[]</code>; audit full returned 0 errors/0 warnings; feature live reached 7/7 fresh. A reviewed Riverboat plan carried one explicitly approved Grok Imagine step with unsafe provenance retained; Grok Build 0.2.102 produced one 1280×720 plate; prompt/YAML/wrapper/binary/output/image hashes were captured; human review promoted the exact file; normal da-cli binary upload installed it in DA; the rendered optimized derivative is public with no <code>about:error</code>. This closes the second-site and credentialed-integration slices, not the whole wave. Evidence pointer: <code>dogfood/evidence/wave-6/</code>.</p></div>
-           <div><p>2026-07-17 01:50</p></div>
+           <div><p>2026-07-17 02:15</p></div>
-           <div><p><strong>The recency audit's shipping run became the next finding — f033, the ghost specimen.</strong> The audit turn observed <code>publish page --format json</code> succeed with empty stdout and filed it as fresh f017 residue. Triage against the current tree showed the envelope already there: PATH <code>da</code> was a <em>fifteen-day-stale</em> global npm install predating the entire f014–f032 flywheel — and both installs report the same <code>0.5.1</code>, because the package deliberately holds its version until this gate opens. Version strings cannot reveal skew on a hold-the-version line (<a href="/learnings/f033-version-blind-binary-skew">f033</a>). Fixed in product: <code>da status</code> now reports <code>data.cli</code> binary provenance — packageRoot, installKind, git sha+dirty. Fixed on the machine: the global install is now an <code>npm link</code> to the working tree. The one friction from that turn that WAS real got closed in the same pass: the f021-residue <code>publish pages</code> variadic asymmetry — the promote step refused the exact page list the preview step accepted — plus its missing dry-run envelope (f017 pattern). Two locks verified failing on old code; 611/611; field-proven: the refused two-path publish now promotes 2/2 and dry-runs to one <code>publish.pages</code> envelope. Residue for the march: committed <code>preview/publish pages</code> success output is still a bare array under json.</p></div>
+           <div><p><strong>Wave 5 cut — the completion drills ate three more primitives.</strong> B1 (source drift): the conflict guard was perfect and its recovery was a trap — <code>content merge</code> destroyed the modified local copy it told you to re-apply (<a href="/learnings/f034-merge-destroys-local-side">f034</a>; merge now preserves the local side under <code>.da/merge-backups/</code>, drift refusals are envelopes). B2 (invalid section shape): the strict door held but <code>--no-strict-sections</code> was a dead switch blaming an absent flag with empty json stdout (<a href="/learnings/f035-broken-off-switch">f035</a>); the flatten it guards against is <em>invisible</em> to rendered-side audits — <code>audit full</code> passed 0-errors over a destroyed block — and <code>preview explain</code> is the recognizer that names it. B3: <code>audit contracts --verify-code</code> named the missing asset exactly. D3 (approval): a closed-stdin gate exited <strong>0</strong> mid-run and stranded a zombie <code>running</code> state (<a href="/learnings/f036-approval-gate-zombie">f036</a>; approvals now explicit via <code>--approve/--approve-all</code>, non-TTY refusals fail fast, prompt on stderr). D4: partial batch failure was honest per-row but exited 0 as a bare array — now one envelope, exit 1. C2's unpublish leg re-probed: still 403, deferred on the operator grant, refusal envelope clean. da-cli 618/618, 7 new locks all verified failing on old code; every fix field-re-proven on this site. Wave 5 <strong>cut</strong> with the C2 leg documented; Wave 6 is the last wave before the gate.</p></div>
-           <div><p>2026-07-17 01:20</p></div>
+           <div><p>2026-07-17 01:50</p></div>
-           <div><p><strong>Recency audit — the catalog caught up with the flywheel.</strong> Site-wide content audit for correctness and recency (all narrative/proof pages checked against <code>dogfood/</code> boards and the friction gate). <a href="/coverage">Coverage</a> was three waves behind the work: added Wave 4 (cut, f021–f026 haul), Wave 5 in-progress (drill ledger, f027–f032), and a Labs section (Dualform, Frontier); learnings line refreshed to f001–f032. <a href="/">Home</a> status line re-synced to this plan (was frozen at Wave 4 opening, claimed f013 open and Waves 5–6 not started). <a href="/route-matrix">Route matrix</a> gains a postscript: its documented probe-gap partial became f013, upgraded High, fixed — the "orphan" tools route now classifies codebus. Pages verified current and left alone: why, blocks, cli-surface (frozen cut ledger), kitchen-sink, dualform, nav, footer, coverage sheet. Stale "As of" stamp on this page's status table also corrected.</p></div>
+           <div><p><strong>The recency audit's shipping run became the next finding — f033, the ghost specimen.</strong> The audit turn observed <code>publish page --format json</code> succeed with empty stdout and filed it as fresh f017 residue. Triage against the current tree showed the envelope already there: PATH <code>da</code> was a <em>fifteen-day-stale</em> global npm install predating the entire f014–f032 flywheel — and both installs report the same <code>0.5.1</code>, because the package deliberately holds its version until this gate opens. Version strings cannot reveal skew on a hold-the-version line (<a href="/learnings/f033-version-blind-binary-skew">f033</a>). Fixed in product: <code>da status</code> now reports <code>data.cli</code> binary provenance — packageRoot, installKind, git sha+dirty. Fixed on the machine: the global install is now an <code>npm link</code> to the working tree. The one friction from that turn that WAS real got closed in the same pass: the f021-residue <code>publish pages</code> variadic asymmetry — the promote step refused the exact page list the preview step accepted — plus its missing dry-run envelope (f017 pattern). Two locks verified failing on old code; 611/611; field-proven: the refused two-path publish now promotes 2/2 and dry-runs to one <code>publish.pages</code> envelope. Residue for the march: committed <code>preview/publish pages</code> success output is still a bare array under json.</p></div>
-           <div><p>2026-07-17 01:05</p></div>
+           <div><p>2026-07-17 01:20</p></div>
-           <div><p><strong>Three more drills, three more findings — the wave keeps eating primitives.</strong> A1 (unresolved target): the truly-unresolved refusal was correct but carried recovery only in prose (<code>next: []</code>) — and the sneakier case, silent GLOBAL-config fallback, listed a <em>different project</em> with zero warnings (<a href="/learnings/f030-silent-global-target">f030</a> — reads now warn, refusals carry executable recovery). A3 (mutation without <code>--commit</code>): clean across content and publish lanes — dry-run envelopes with exact promotion commands, remote 404 proves containment. C1 (stale preview/live): the drill's first honest result was the finding — <code>site freshness</code> could <em>never</em> say preview-stale because DA's ms-epoch timestamps normalized to empty and the row compared the preview against its own snapshot (<a href="/learnings/f031-freshness-blind-to-staleness">f031</a> — fixed, full ladder field-proven: preview-missing → preview-only → preview-stale → live-stale → fresh). The cleanup leg hit the token's unpublish 403 and left a REAL orphan — C2's injection for free: <code>route classify</code> recognized it (exit 2, <code>staleDaLocation</code>), source restore recovered it; final removal waits on permissions. And the day's three empty-stdout refusals became <a href="/learnings/f032-silent-refusals">f032</a>: the <code>prepareWrite</code> seam now envelopes every safety refusal. da-cli suite 609/609. Wave 5: A1 A2 A3 C1 C3 closed, C2 ◐, B1–B3 + D3/D4 fresh reps remain.</p></div>
+           <div><p><strong>Recency audit — the catalog caught up with the flywheel.</strong> Site-wide content audit for correctness and recency (all narrative/proof pages checked against <code>dogfood/</code> boards and the friction gate). <a href="/coverage">Coverage</a> was three waves behind the work: added Wave 4 (cut, f021–f026 haul), Wave 5 in-progress (drill ledger, f027–f032), and a Labs section (Dualform, Frontier); learnings line refreshed to f001–f032. <a href="/">Home</a> status line re-synced to this plan (was frozen at Wave 4 opening, claimed f013 open and Waves 5–6 not started). <a href="/route-matrix">Route matrix</a> gains a postscript: its documented probe-gap partial became f013, upgraded High, fixed — the "orphan" tools route now classifies codebus. Pages verified current and left alone: why, blocks, cli-surface (frozen cut ledger), kitchen-sink, dualform, nav, footer, coverage sheet. Stale "As of" stamp on this page's status table also corrected.</p></div>
-           <div><p>2026-07-17 00:25</p></div>
+           <div><p>2026-07-17 01:05</p></div>
-           <div><p><strong>Drill C3 closed — the live site was the injection.</strong> The freshly published f027 page never entered the query index; <code>maxLastModified</code> was frozen; wildcard reindex jobs were accepted with zero effect. Two findings underneath: <a href="/learnings/f028-config-read-gated-build">f028</a> — <code>index build --wait</code> died on an unauthorized Configuration Service <em>verification read</em> while the rebuild itself was authorized (raw POST → 202), blaming <code>*</code> instead of the config route (fixed: config read is best-effort with the denied route named; new <code>DA_PUBLIC_HOST_URL</code> hermetic seam; 602/602). And <a href="/learnings/f029-index-config-false-migration">f029</a> — the previous session's "config-native" index migration was claimed in docs but never observed on the platform: the deleted <code>helix-query.yaml</code> was the ACTIVE config and live indexing had silently de-configured (fossil index kept serving; <code>#simple</code>-only reindex match was the tell). Recovered: yaml restored, codebus parity confirmed, named-index match returned, rebuild <code>built-and-verified</code>, <code>total 39→40, changed:true</code>, f027 now live in the index. Boards/provenance reverted to observed truth; discipline stated: platform-state claims require live-response proof. Residue: <code>code sync</code> safety refusal is empty-stdout under json (f017 class); <code>index show/validate</code> can't observe config truth without Configuration Service access (D6 fitness gap).</p></div>
+           <div><p><strong>Three more drills, three more findings — the wave keeps eating primitives.</strong> A1 (unresolved target): the truly-unresolved refusal was correct but carried recovery only in prose (<code>next: []</code>) — and the sneakier case, silent GLOBAL-config fallback, listed a <em>different project</em> with zero warnings (<a href="/learnings/f030-silent-global-target">f030</a> — reads now warn, refusals carry executable recovery). A3 (mutation without <code>--commit</code>): clean across content and publish lanes — dry-run envelopes with exact promotion commands, remote 404 proves containment. C1 (stale preview/live): the drill's first honest result was the finding — <code>site freshness</code> could <em>never</em> say preview-stale because DA's ms-epoch timestamps normalized to empty and the row compared the preview against its own snapshot (<a href="/learnings/f031-freshness-blind-to-staleness">f031</a> — fixed, full ladder field-proven: preview-missing → preview-only → preview-stale → live-stale → fresh). The cleanup leg hit the token's unpublish 403 and left a REAL orphan — C2's injection for free: <code>route classify</code> recognized it (exit 2, <code>staleDaLocation</code>), source restore recovered it; final removal waits on permissions. And the day's three empty-stdout refusals became <a href="/learnings/f032-silent-refusals">f032</a>: the <code>prepareWrite</code> seam now envelopes every safety refusal. da-cli suite 609/609. Wave 5: A1 A2 A3 C1 C3 closed, C2 ◐, B1–B3 + D3/D4 fresh reps remain.</p></div>
-           <div><p>2026-07-16 23:55</p></div>
+           <div><p>2026-07-17 00:25</p></div>
-           <div><p><strong>Wave 5 opened — the first drill injected itself.</strong> The session began with a naturally expired token, which IS drill A2 (missing/expired auth): the read attempt failed as exactly one <code>ok:false</code> envelope with executable recovery (<code>da auth login</code>), exit 1; explicit login completed from the agent's own non-TTY shell (the <a href="/learnings/f020-non-tty-auth-hang">f020</a> path); and the recovery-verification read then exposed <a href="/learnings/f027-list-success-bare-array">f027</a> — <code>content list</code> <em>succeeded</em> as a bare array, so the agent could parse its failure but not verify its recovery. Fixed + locked same-session (read-listing family now emits one envelope on success, empty results included; new <code>DA_ADMIN_URL</code> hermetic seam; 600/600, pushed to da-cli main). Wave 5 drill matrix on the wave board: A1–D4 with banked Wave 4 specimens (f022 interruption, f024 partial batch, f025 abort) credited by pointer. Residue watch: <code>status</code> reports <code>ok:false</code> on expired auth but exits 0 — classify before cut. Also landed: index config went config-native (Configuration Service <code>content/query.yaml</code>; repo-local <code>helix-query.yaml</code> retired per ADR 0002 D6/G6).</p></div>
+           <div><p><strong>Drill C3 closed — the live site was the injection.</strong> The freshly published f027 page never entered the query index; <code>maxLastModified</code> was frozen; wildcard reindex jobs were accepted with zero effect. Two findings underneath: <a href="/learnings/f028-config-read-gated-build">f028</a> — <code>index build --wait</code> died on an unauthorized Configuration Service <em>verification read</em> while the rebuild itself was authorized (raw POST → 202), blaming <code>*</code> instead of the config route (fixed: config read is best-effort with the denied route named; new <code>DA_PUBLIC_HOST_URL</code> hermetic seam; 602/602). And <a href="/learnings/f029-index-config-false-migration">f029</a> — the previous session's "config-native" index migration was claimed in docs but never observed on the platform: the deleted <code>helix-query.yaml</code> was the ACTIVE config and live indexing had silently de-configured (fossil index kept serving; <code>#simple</code>-only reindex match was the tell). Recovered: yaml restored, codebus parity confirmed, named-index match returned, rebuild <code>built-and-verified</code>, <code>total 39→40, changed:true</code>, f027 now live in the index. Boards/provenance reverted to observed truth; discipline stated: platform-state claims require live-response proof. Residue: <code>code sync</code> safety refusal is empty-stdout under json (f017 class); <code>index show/validate</code> can't observe config truth without Configuration Service access (D6 fitness gap).</p></div>
-           <div><p>2026-07-16 17:30</p></div>
+           <div><p>2026-07-16 23:55</p></div>
-           <div><p><strong>Wave 4 cut — the drills ate their own primitives, four times.</strong> Durable jobs: SIGKILL at 7/21 → resume crashed on the dead runner's lock (<a href="/learnings/f022-stale-job-lock">f022</a>, Critical — fixed with pid-liveness reclaim; resume → 21/21, attempts all 1). Migration: explicit <code>--path</code> imported unservable content as 'imported' (<a href="/learnings/f023-migrate-path-normalization">f023</a>); colliding batch lines silently overwrote each other, all 'done' (<a href="/learnings/f024-batch-collision-overwrite">f024</a> — now refuses with the url→path table). Pipelines: <code>pipeline abort</code> was clobbered by the runner's own per-step saves — runs completed <em>through</em> their own abort (<a href="/learnings/f025-abort-marker-clobbered">f025</a>; field drill on the real 74-step regen: 11 completed, 63 never ran, marker preserved through a step save landing 1.8s post-abort). Recovery rerun 74/74; idempotence re-proven at current CLI. And shipping the cut ate one more primitive: <code>deploy pages</code> wrote two bare arrays to json stdout — unparseable flawless deploy (<a href="/learnings/f026-deploy-double-envelope">f026</a>, fixed + locked before this entry was published; re-deploy 6/6, one envelope). All five fixed + locked in local 0.5.x same-session (578/578, CI ≤35s). Surface table on the wave board; evidence: <code>dogfood/evidence/wave-4/</code>. Next: Wave 5 failure-injection — five specimens already banked.</p></div>
+           <div><p><strong>Wave 5 opened — the first drill injected itself.</strong> The session began with a naturally expired token, which IS drill A2 (missing/expired auth): the read attempt failed as exactly one <code>ok:false</code> envelope with executable recovery (<code>da auth login</code>), exit 1; explicit login completed from the agent's own non-TTY shell (the <a href="/learnings/f020-non-tty-auth-hang">f020</a> path); and the recovery-verification read then exposed <a href="/learnings/f027-list-success-bare-array">f027</a> — <code>content list</code> <em>succeeded</em> as a bare array, so the agent could parse its failure but not verify its recovery. Fixed + locked same-session (read-listing family now emits one envelope on success, empty results included; new <code>DA_ADMIN_URL</code> hermetic seam; 600/600, pushed to da-cli main). Wave 5 drill matrix on the wave board: A1–D4 with banked Wave 4 specimens (f022 interruption, f024 partial batch, f025 abort) credited by pointer. Residue watch: <code>status</code> reports <code>ok:false</code> on expired auth but exits 0 — classify before cut. Also landed: index config went config-native (Configuration Service <code>content/query.yaml</code>; repo-local <code>helix-query.yaml</code> retired per ADR 0002 D6/G6).</p></div>
-           <div><p>2026-07-16 03:30</p></div>
+           <div><p>2026-07-16 17:30</p></div>
-           <div><p><strong>Wave 4 opened — the YAML surface held.</strong> Ran the full committed <code>construct.yaml</code> (58 steps) against the live site <em>twice</em> (runs <code>7165cc1f</code>, <code>d691ab30</code>): all 28 public routes byte-identical before and after both regenerations — the site is provably regenerable and the pipeline is idempotent. The rep surfaced <a href="/learnings/f014-pipeline-json-stdout">f014</a>: child-step stdout polluted the parent's <code>--format json</code> envelope (perfect run, unparseable output). Fixed same session — JSON parent routes child stdout to stderr — locked e2e (524/524), field-proven on the second run whose raw stdout parses clean. Evidence: <code>dogfood/evidence/wave-4/</code>. Remaining for Wave 4: durable jobs (<code>pipeline run --job</code> does not exist yet), interruption survival, migration. Next friction candidates noted in <code>dogfood/FRICTION-GATE-0.6.0.md</code>: put/preview/publish envelope gaps, <code>index build --wait</code>.</p></div>
+           <div><p><strong>Wave 4 cut — the drills ate their own primitives, four times.</strong> Durable jobs: SIGKILL at 7/21 → resume crashed on the dead runner's lock (<a href="/learnings/f022-stale-job-lock">f022</a>, Critical — fixed with pid-liveness reclaim; resume → 21/21, attempts all 1). Migration: explicit <code>--path</code> imported unservable content as 'imported' (<a href="/learnings/f023-migrate-path-normalization">f023</a>); colliding batch lines silently overwrote each other, all 'done' (<a href="/learnings/f024-batch-collision-overwrite">f024</a> — now refuses with the url→path table). Pipelines: <code>pipeline abort</code> was clobbered by the runner's own per-step saves — runs completed <em>through</em> their own abort (<a href="/learnings/f025-abort-marker-clobbered">f025</a>; field drill on the real 74-step regen: 11 completed, 63 never ran, marker preserved through a step save landing 1.8s post-abort). Recovery rerun 74/74; idempotence re-proven at current CLI. And shipping the cut ate one more primitive: <code>deploy pages</code> wrote two bare arrays to json stdout — unparseable flawless deploy (<a href="/learnings/f026-deploy-double-envelope">f026</a>, fixed + locked before this entry was published; re-deploy 6/6, one envelope). All five fixed + locked in local 0.5.x same-session (578/578, CI ≤35s). Surface table on the wave board; evidence: <code>dogfood/evidence/wave-4/</code>. Next: Wave 5 failure-injection — five specimens already banked.</p></div>
-           <div><p>2026-07-16 03:10</p></div>
+           <div><p>2026-07-16 03:30</p></div>
-           <div><p><strong>Ecosystem trajectory absorbed</strong> into positioning: AI authoring surfaces arriving on DA, and AI personalization at the delivery edge (<a href="https://of1.live/">of1</a>, early-stage Adobe AEM tech with Stardust provenance markers). <a href="/why">Why</a> extended; da-cli git <code>docs/BOUNDARIES.md</code> gains a trajectory section, personalization-scoped proof semantics (canonical bus only, per-visitor variants out of contract), and roadmap bucket 6: AI-era proof surfaces / provenance verification.</p></div>
+           <div><p><strong>Wave 4 opened — the YAML surface held.</strong> Ran the full committed <code>construct.yaml</code> (58 steps) against the live site <em>twice</em> (runs <code>7165cc1f</code>, <code>d691ab30</code>): all 28 public routes byte-identical before and after both regenerations — the site is provably regenerable and the pipeline is idempotent. The rep surfaced <a href="/learnings/f014-pipeline-json-stdout">f014</a>: child-step stdout polluted the parent's <code>--format json</code> envelope (perfect run, unparseable output). Fixed same session — JSON parent routes child stdout to stderr — locked e2e (524/524), field-proven on the second run whose raw stdout parses clean. Evidence: <code>dogfood/evidence/wave-4/</code>. Remaining for Wave 4: durable jobs (<code>pipeline run --job</code> does not exist yet), interruption survival, migration. Next friction candidates noted in <code>dogfood/FRICTION-GATE-0.6.0.md</code>: put/preview/publish envelope gaps, <code>index build --wait</code>.</p></div>
-           <div><p>2026-07-16 03:05</p></div>
+           <div><p>2026-07-16 03:10</p></div>
-           <div><p><strong>Cut-sync + flywheel housekeeping</strong> (fresh-eyes review pass, executed through <code>da</code>). Published <a href="/learnings/f012-da-permission-headers">f012</a> (was fixed + merged in da-cli but never wired to the hub/construct — last-mile gap); logged <a href="/learnings/f013-tools-classify-probe-gap">f013</a> as <strong>open</strong> (classify probe gap for codebus static <code>/tools/*.html</code>, promoted from a route-matrix caveat to a numbered learning). Synced the home status line to the Wave 3 cut and made <a href="/test-plan">test plan</a> the declared source of truth for wave states. Re-stamped <code>status=cut</code> metadata on <a href="/kitchen-sink">kitchen-sink</a>, <a href="/blocks">blocks</a>, <a href="/coverage">coverage</a>; index rebuilt to pick up new rows. Added ecosystem-position note on <a href="/why">why</a> (da-cli is independent, complementary to the canonical <code>aem</code> CLI — not Adobe canon). Package stays 0.5.x.</p></div>
+           <div><p><strong>Ecosystem trajectory absorbed</strong> into positioning: AI authoring surfaces arriving on DA, and AI personalization at the delivery edge (<a href="https://of1.live/">of1</a>, early-stage Adobe AEM tech with Stardust provenance markers). <a href="/why">Why</a> extended; da-cli git <code>docs/BOUNDARIES.md</code> gains a trajectory section, personalization-scoped proof semantics (canonical bus only, per-visitor variants out of contract), and roadmap bucket 6: AI-era proof surfaces / provenance verification.</p></div>
-           <div><p>2026-07-14 03:05</p></div>
+           <div><p>2026-07-16 03:05</p></div>
-           <div><p><strong>Wave 3 cut.</strong> Prove the site <em>map</em>: <code>content sheets</code> (3), index show/validate/query/build, route audit (54 paths) + classify matrix (contentbus / codebus / hybrid / orphan), <code>route clean</code> dry-run on orphan demo. Public <a href="/route-matrix">/route-matrix</a> live 200 and in query-index (23→24 rows after build). Evidence: <code>dogfood/evidence/wave-3/</code>. Documented partial: tools static classify probe gap (live 200, classify 404s — not a clean candidate). Package stays 0.5.x — not a 0.6.0 release cut. Next: Wave 4 pipelines.</p></div>
+           <div><p><strong>Cut-sync + flywheel housekeeping</strong> (fresh-eyes review pass, executed through <code>da</code>). Published <a href="/learnings/f012-da-permission-headers">f012</a> (was fixed + merged in da-cli but never wired to the hub/construct — last-mile gap); logged <a href="/learnings/f013-tools-classify-probe-gap">f013</a> as <strong>open</strong> (classify probe gap for codebus static <code>/tools/*.html</code>, promoted from a route-matrix caveat to a numbered learning). Synced the home status line to the Wave 3 cut and made <a href="/test-plan">test plan</a> the declared source of truth for wave states. Re-stamped <code>status=cut</code> metadata on <a href="/kitchen-sink">kitchen-sink</a>, <a href="/blocks">blocks</a>, <a href="/coverage">coverage</a>; index rebuilt to pick up new rows. Added ecosystem-position note on <a href="/why">why</a> (da-cli is independent, complementary to the canonical <code>aem</code> CLI — not Adobe canon). Package stays 0.5.x.</p></div>
-           <div><p>2026-07-14 02:51</p></div>
+           <div><p>2026-07-14 03:05</p></div>
-           <div><p><strong>Wave 2 cut.</strong> Re-ran audits into <code>dogfood/evidence/wave-2/</code>: <code>audit contracts --verify-code</code> exit 0 (23 pages, 19 blocks, missing=[], autoblocks modal/fragment/widget); <code>audit full /kitchen-sink</code> ok errors 0; <code>design detect</code> clean; <code>design audit</code> exit 0; token-check <code>no-design-tokens</code> intentional. Proof: <a href="/kitchen-sink">/kitchen-sink</a> + <a href="/cli-surface">/cli-surface</a>. Documented partial: block inspect sparse. Friction column already clear. Next: Wave 3. Package stays 0.5.x — not a 0.6.0 release cut.</p></div>
+           <div><p><strong>Wave 3 cut.</strong> Prove the site <em>map</em>: <code>content sheets</code> (3), index show/validate/query/build, route audit (54 paths) + classify matrix (contentbus / codebus / hybrid / orphan), <code>route clean</code> dry-run on orphan demo. Public <a href="/route-matrix">/route-matrix</a> live 200 and in query-index (23→24 rows after build). Evidence: <code>dogfood/evidence/wave-3/</code>. Documented partial: tools static classify probe gap (live 200, classify 404s — not a clean candidate). Package stays 0.5.x — not a 0.6.0 release cut. Next: Wave 4 pipelines.</p></div>
-           <div><p>2026-07-14 02:45</p></div>
+           <div><p>2026-07-14 02:51</p></div>
-           <div><p><strong>CLI surface ledger</strong> for Wave 2 formal cut: <a href="/cli-surface">/cli-surface</a> (human journeys A–D) + interactive <a href="/tools/wave-2-cli-surface.html">tools/wave-2-cli-surface</a> (flows-inspired, dogfood-scoped) + sheet <a href="/data/wave-2-cli-surface.json">wave-2-cli-surface.json</a>. Maps commands → proof targets → evidence — the addendum missing from kitchen-sink alone.</p></div>
+           <div><p><strong>Wave 2 cut.</strong> Re-ran audits into <code>dogfood/evidence/wave-2/</code>: <code>audit contracts --verify-code</code> exit 0 (23 pages, 19 blocks, missing=[], autoblocks modal/fragment/widget); <code>audit full /kitchen-sink</code> ok errors 0; <code>design detect</code> clean; <code>design audit</code> exit 0; token-check <code>no-design-tokens</code> intentional. Proof: <a href="/kitchen-sink">/kitchen-sink</a> + <a href="/cli-surface">/cli-surface</a>. Documented partial: block inspect sparse. Friction column already clear. Next: Wave 3. Package stays 0.5.x — not a 0.6.0 release cut.</p></div>
-           <div><p>2026-07-14 02:31</p></div>
+           <div><p>2026-07-14 02:45</p></div>
-           <div><p><strong>Friction column clear.</strong> Closed f011 (contracts inventory modal/widget/fragment autoblocks + verify-code). Also this session: f002 design envelopes, f008 subtree merge, f009 ownership put flag, f010 media URL warn/rewrite/strict. Honest gate: agentic frictions f001–f011 fixed in local 0.5.x; Wave 2 not formally cut; Waves 3–6 incomplete; package stays 0.5.x — <strong>do not cut 0.6.0 yet</strong>. See <code>dogfood/FRICTION-GATE-0.6.0.md</code>.</p></div>
+           <div><p><strong>CLI surface ledger</strong> for Wave 2 formal cut: <a href="/cli-surface">/cli-surface</a> (human journeys A–D) + interactive <a href="/tools/wave-2-cli-surface.html">tools/wave-2-cli-surface</a> (flows-inspired, dogfood-scoped) + sheet <a href="/data/wave-2-cli-surface.json">wave-2-cli-surface.json</a>. Maps commands → proof targets → evidence — the addendum missing from kitchen-sink alone.</p></div>
-           <div><p>2026-07-14 03:00</p></div>
+           <div><p>2026-07-14 02:31</p></div>
-           <div><p><strong>f008 fixed</strong> in da-cli: <code>content merge /folder</code> refreshes subtree documents (tracked ∪ remote list), never bare-folder <code>sourceGet</code>. Path semantics match clone. Locks in <code>content-workspace.test.js</code>. Critical friction gate closed; remaining open: f002, f009–f011.</p></div>
+           <div><p><strong>Friction column clear.</strong> Closed f011 (contracts inventory modal/widget/fragment autoblocks + verify-code). Also this session: f002 design envelopes, f008 subtree merge, f009 ownership put flag, f010 media URL warn/rewrite/strict. Honest gate: agentic frictions f001–f011 fixed in local 0.5.x; Wave 2 not formally cut; Waves 3–6 incomplete; package stays 0.5.x — <strong>do not cut 0.6.0 yet</strong>. See <code>dogfood/FRICTION-GATE-0.6.0.md</code>.</p></div>
-           <div><p>2026-07-14 02:15</p></div>
+           <div><p>2026-07-14 03:00</p></div>
-           <div><p><strong>Friction gate formalized</strong> as the hang-hat for 0.6.0: <code>dogfood/FRICTION-GATE-0.6.0.md</code>. Open Critical/High learnings (f002, f008–f011) block the cut even if waves are green. Clone/merge path semantics compared to helix-cli (<code>content/</code> + real git + 3-way merge) vs da-cli (<code>.da/workspace/</code> + hash state + single-URL merge) — f008 called as implementation defect, not operator error.</p></div>
+           <div><p><strong>f008 fixed</strong> in da-cli: <code>content merge /folder</code> refreshes subtree documents (tracked ∪ remote list), never bare-folder <code>sourceGet</code>. Path semantics match clone. Locks in <code>content-workspace.test.js</code>. Critical friction gate closed; remaining open: f002, f009–f011.</p></div>
-           <div><p>2026-07-14 01:35</p></div>
+           <div><p>2026-07-14 02:15</p></div>
-           <div><p>Learnings f008–f011 logged (merge path 404, put outside worktree, media about:error, contracts miss autoblocks) — open agentic CLI frictions for 0.6.0.</p></div>
+           <div><p><strong>Friction gate formalized</strong> as the hang-hat for 0.6.0: <code>dogfood/FRICTION-GATE-0.6.0.md</code>. Open Critical/High learnings (f002, f008–f011) block the cut even if waves are green. Clone/merge path semantics compared to helix-cli (<code>content/</code> + real git + 3-way merge) vs da-cli (<code>.da/workspace/</code> + hash state + single-URL merge) — f008 called as implementation defect, not operator error.</p></div>
-           <div><p>2026-07-14 01:30</p></div>
+           <div><p>2026-07-14 01:35</p></div>
-           <div><p><strong>Wave 1 cut.</strong> Closed foundation &amp; core loop: pin-target + <code>.da.json</code>, site model, content clone/diff/merge, put-tree dry-run, construct dry-run (42 steps), <code>aem up</code> HTTP 200 smoke, full evidence pack at <code>dogfood/evidence/wave-1/</code>.</p></div>
+           <div><p>Learnings f008–f011 logged (merge path 404, put outside worktree, media about:error, contracts miss autoblocks) — open agentic CLI frictions for 0.6.0.</p></div>
-           <div><p>2026-07-14 01:27</p></div>
+           <div><p>2026-07-14 01:30</p></div>
-           <div><p>Test plan rewritten with status table, progress log, and honest wave states; last-updated discipline documented on this page.</p></div>
+           <div><p><strong>Wave 1 cut.</strong> Closed foundation &amp; core loop: pin-target + <code>.da.json</code>, site model, content clone/diff/merge, put-tree dry-run, construct dry-run (42 steps), <code>aem up</code> HTTP 200 smoke, full evidence pack at <code>dogfood/evidence/wave-1/</code>.</p></div>
-           <div><p>2026-07-14 01:20</p></div>
+           <div><p>2026-07-14 01:27</p></div>
-           <div><p>Nav restructured with Story / Prove / Learnings / Labs submenus so proof and history stay reachable without a flat link dump.</p></div>
+           <div><p>Test plan rewritten with status table, progress log, and honest wave states; last-updated discipline documented on this page.</p></div>
-           <div><p>2026-07-14 01:15</p></div>
+           <div><p>2026-07-14 01:20</p></div>
-           <div><p>Worker stubs documented and shipped (<code>form-submit</code>, <code>auth-session</code>); <code>n8n-form</code> and <code>auth-toggle</code> stubs on kitchen-sink; wrangler notes under <code>workers/README.md</code>.</p></div>
+           <div><p>Nav restructured with Story / Prove / Learnings / Labs submenus so proof and history stay reachable without a flat link dump.</p></div>
-           <div><p>2026-07-14 01:00</p></div>
+           <div><p>2026-07-14 01:15</p></div>
-           <div><p>Field-scale block spectrum on kitchen-sink: form, search, modal, teaser, accordion, carousel, quote, tabs, table, embed; <code>BLOCK-COVERAGE.md</code> as cut gate; contracts audit green for kitchen-sink blocks.</p></div>
+           <div><p>Worker stubs documented and shipped (<code>form-submit</code>, <code>auth-session</code>); <code>n8n-form</code> and <code>auth-toggle</code> stubs on kitchen-sink; wrangler notes under <code>workers/README.md</code>.</p></div>
-           <div><p>2026-07-14 00:50</p></div>
+           <div><p>2026-07-14 01:00</p></div>
-           <div><p>Content IA: narrative / proof / learning / lab. Learnings hub + f001–f007 individual routes (legacy <code>/findings</code> later removed).</p></div>
+           <div><p>Field-scale block spectrum on kitchen-sink: form, search, modal, teaser, accordion, carousel, quote, tabs, table, embed; <code>BLOCK-COVERAGE.md</code> as cut gate; contracts audit green for kitchen-sink blocks.</p></div>
-           <div><p>2026-07-13 ~21:00</p></div>
+           <div><p>2026-07-14 00:50</p></div>
-           <div><p>Wave board + kitchen-sink MVP; ported collection blocks (accordion, tabs, carousel, table, embed, quote); evidence folders under <code>dogfood/evidence/</code>.</p></div>
+           <div><p>Content IA: narrative / proof / learning / lab. Learnings hub + f001–f007 individual routes (legacy <code>/findings</code> later removed).</p></div>
-           <div><p>2026-07-13 ~20:00</p></div>
+           <div><p>2026-07-13 ~21:00</p></div>
-           <div><p>Renderaissance home hero: soft plate + lit PE; dualform lab already on site from earlier PE work.</p></div>
+           <div><p>Wave board + kitchen-sink MVP; ported collection blocks (accordion, tabs, carousel, table, embed, quote); evidence folders under <code>dogfood/evidence/</code>.</p></div>
-           <div><p>Earlier 2026-07</p></div>
+           <div><p>2026-07-13 ~20:00</p></div>
-           <div><p>Site scaffold, construct pipeline, waves 1–3 narrative pages, learnings f001–f007 from field dogfood (site info, design detect, index, route classify/clean).</p></div>
+           <div><p>Renderaissance home hero: soft plate + lit PE; dualform lab already on site from earlier PE work.</p></div>
-       </div>
+         <div>
-     </div>
+           <div><p>Earlier 2026-07</p></div>
- 
+           <div><p>Site scaffold, construct pipeline, waves 1–3 narrative pages, learnings f001–f007 from field dogfood (site info, design detect, index, route classify/clean).</p></div>
-     <div>
+         </div>
-       <h2>How to keep this page honest</h2>
+       </div>
-       <ol>
+     </div>
-         <li>Clone or merge authoritative DA source with <code>da content clone</code> / <code>da content merge</code>.</li>
+ 
-         <li>Edit <code>.da/workspace/test-plan.html</code>, then use the da-cli add/commit/push workflow.</li>
+     <div>
-         <li>Set hero <strong>Last updated</strong> to current <code>YYYY-MM-DD HH:MM UTC</code> and prepend the same timestamp to the progress log.</li>
+       <h2>How to keep this page honest</h2>
-         <li>Adjust the status table only when a wave state actually changes.</li>
+       <ol>
-         <li>Run <code>dogfood/certify.yaml</code>; promote separately through <code>dogfood/promote.yaml</code> after review.</li>
+         <li>Clone or merge authoritative DA source with <code>da content clone</code> / <code>da content merge</code>.</li>
-         <li>Retain machine evidence under <code>dogfood/evidence/wave-N/</code>; never mirror authored DA documents into Git.</li>
+         <li>Edit <code>.da/workspace/test-plan.html</code>, then use the da-cli add/commit/push workflow.</li>
-       </ol>
+         <li>Set hero <strong>Last updated</strong> to current <code>YYYY-MM-DD HH:MM UTC</code> and prepend the same timestamp to the progress log.</li>
-     </div>
+         <li>Adjust the status table only when a wave state actually changes.</li>
- 
+         <li>Run <code>dogfood/certify.yaml</code>; promote separately through <code>dogfood/promote.yaml</code> after review.</li>
-     <div>
+         <li>Retain machine evidence under <code>dogfood/evidence/wave-N/</code>; never mirror authored DA documents into Git.</li>
-       <h2>The gate</h2>
+       </ol>
-       <p>Two columns — both required for a hat-worthy 0.6.0:</p>
+     </div>
-       <ol>
+ 
-         <li><strong>Waves:</strong> every required surface green with stored evidence; DA-native certification and promotion verify the site; provenance resolves.</li>
+     <div>
-         <li><strong>Frictions:</strong> every Critical/High agentic friction closed — see git <code>dogfood/FRICTION-GATE-0.6.0.md</code> and <a href="/learnings">/learnings</a> (f001–f012 fixed; f013 open at Medium, tracked).</li>
+       <h2>The gate</h2>
-       </ol>
+       <p>Two columns — both required for a hat-worthy 0.6.0:</p>
-       <p>Waves without friction fixes are not enough. Friction fixes without wave evidence are not enough.</p>
+       <ol>
-       <p>As of <strong>2026-07-23 13:42 UTC</strong>: <strong>friction column clear</strong> for the release-scoped boundary; Waves 1–6 are <strong>cut</strong> with retained evidence. Wave 5 still carries the explicit C2 operator decision tracked in the release gate. The wave column is clear, but the <strong>0.6.0 release is not cut yet</strong>: strict DA-native provenance freeze, the C2 decision, final docs-site human review, live signoff, and npm/GitHub verification remain.</p>
+         <li><strong>Waves:</strong> every required surface green with stored evidence; DA-native certification and promotion verify the site; provenance resolves.</li>
-     </div>
+         <li><strong>Frictions:</strong> every Critical/High agentic friction closed — see git <code>dogfood/FRICTION-GATE-0.6.0.md</code> and <a href="/learnings">/learnings</a> (f001–f012 fixed; f013 open at Medium, tracked).</li>
- 
+       </ol>
-     <div>
+       <p>Waves without friction fixes are not enough. Friction fixes without wave evidence are not enough.</p>
-       <div class="metadata">
+       <p>As of <strong>2026-07-23 16:41 UTC</strong>: <strong>friction column clear</strong> for the release-scoped boundary; Waves 1–6 are <strong>cut</strong> with retained evidence, and Wave 5 C2 is closed by isolated orphan-removal and full public-absence proof. The wave column is clear, but the <strong>0.6.0 release is not cut yet</strong>: final strict DA-native provenance bindings/tags, final docs-site human review, live signoff, and npm/GitHub verification remain.</p>
-         <div>
+     </div>
-           <div>kind</div>
+ 
-           <div>narrative</div>
+     <div>
-         </div>
+       <div class="metadata">
-           <div>wave</div>
+           <div>kind</div>
... diff truncated: 38 more changed line(s) omitted ...
Write preflight:
  Target: somarc/da-cli-0-6-0
  Branch: main
  Config: org=flag, repo=flag, branch=flag
  Operation: content put /test-plan.html
  Source: .da/workspace/test-plan.html
  Paths: 1
    /test-plan.html
  Note: Render preflight: 9 section(s), 4 block(s).
Uploaded /test-plan.html
